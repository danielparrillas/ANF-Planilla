import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();

export const createPlanilla = async (data: any) => {
//   return await prisma.planilla.create({
//     data,
//   });
};

export const getPlanillas = async () => {
//   return await prisma.planilla.findMany();
};
